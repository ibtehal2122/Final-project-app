apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: final-project-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ibtehal2122/final-project-app.git  # 👈 لينك الريبو الجديدة
    targetRevision: main
    path: helm  # 👈 المسار اللي فيه الـ Chart.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: final-project # 👈 الـ Namespace اللي التطبيق هينزل فيه
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true # هيعمل الـ namespace لو مش موجودة